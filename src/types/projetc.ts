export interface Project {
    title: string;
    description: string;
    image: string;
    category: string;
    githubLink: string;
    features?: string;
    technologies: string[];
    featured?: boolean;
  }